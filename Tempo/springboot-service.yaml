apiVersion: v1
kind: Service
metadata:
  name: springboot-service
  namespace: app
  labels:
    app: springboot
spec:
  selector:
    app: springboot
  ports:
    - name: http
      port: 80
      targetPort: 8080