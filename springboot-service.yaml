apiVersion: v1
kind: Service
metadata:
  name: springboot-service
  namespace: app
  labels:
    app: springboot   # IMPORTANT
spec:
  selector:
    app: springboot
  ports:
    - name: http      # 🚨 REQUIRED for ServiceMonitor
      port: 80
      targetPort: 8080